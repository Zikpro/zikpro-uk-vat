[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations

[post_model_sync]
# zikpro_uk_vat.patches.v1_0.add_last_2fa_field
zikpro_uk_vat.patches.v1_0.add_eu_vat_fields
# B101: after_migrate doesn't run on Frappe Cloud — pin the timezone via a patch too.
zikpro_uk_vat.patches.v1_0.enforce_uk_timezone
# B101 + Cloud-DocPerm: FC skips tabDocPerm re-apply, so land the VAT DocPerm baseline
# (incl. System Manager read on the return — else the VAT Return report 403s) via a patch.
zikpro_uk_vat.patches.v1_0.ensure_vat_docperms
# Re-reconcile after adding report+export ptypes to UK MTD VAT Return (the prior patch
# already ran once; ensure_vat_docperms won't re-fire, so a new patch re-lands the grant).
zikpro_uk_vat.patches.v1_0.grant_return_report_ptype
# VAT Settings shipped hash-named; on a multi-company app that's unreadable and
# lets two settings exist per company. Doctype now autonames field:company; this
# renames existing hash records to their company (enforces one-per-company).
zikpro_uk_vat.patches.v1_0.rename_vat_settings_to_company